import { style } from 'typestyle';

namespace styles {
	export const container = style({
		display: 'flex',
		flexDirection: 'column',
		width: '100%'
	});
	export const logoWeb = style({
		alignSelf: 'center',
		height: '148px'
	});
}

export default styles;
